{
  "name": "qwen-auth",
  "version": "1.0.1",
  "description": "Authentication plugin for OpenCode that supports Qwen models with OAuth, JWT, and API key authentication",
  "module": "index.ts",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "license": "MIT",
  "author": "lion-lef",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lion-lef/qwen-auth-opencode.git"
  },
  "homepage": "https://github.com/lion-lef/qwen-auth-opencode#readme",
  "bugs": {
    "url": "https://github.com/lion-lef/qwen-auth-opencode/issues"
  },
  "keywords": [
    "opencode",
    "qwen",
    "alibaba",
    "auth",
    "oauth",
    "jwt",
    "api-key",
    "plugin",
    "authentication"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "index.ts",
    "src",
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:plugin": "bun run scripts/build-plugin.ts",
    "build:all": "bun run build && bun run build:plugin",
    "build:schema": "bun run scripts/build-schema.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "prepublishOnly": "bun run build",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write \"src/**/*.ts\" \"index.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"index.ts\""
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.54.0"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.1.35",
    "jose": "^5.2.0",
    "zod": "^3.24.0"
  }
}
